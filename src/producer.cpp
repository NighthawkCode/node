// Producer.cpp : Code to produce data
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
    printf("Hi, I am a producer\n");

    // Allocate the rinbuffer of data, specifying the type, quantity, etc

    // Get the an element

    // Initialize the element (some number or sequence)

    // Release the element
    
    return 0;
}